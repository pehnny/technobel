def find_free_slots(
    activities: list[tuple[int, int]],
    day_start: int = 8,
    day_end: int = 18
) -> list[tuple[int, int]]:
    """
    Retourne les plages horaires libres dans la journée.

    :param activities: liste de tuples (debut, fin) en heures entières
    :param day_start: début de la journée
    :param day_end: fin de la journée
    :return: liste de tuples (debut, fin) représentant les plages libres, triées
    """
    pass
